# Batch 5: Zod Request Validation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace manual `if (!field)` guards in all backend controllers with Zod schema validation at the route middleware layer.

**Architecture:** A single `validate` Express middleware function takes a Zod schema and returns 400 with structured errors on failure. Per-domain schemas live in `server/src/schemas/`. Routes in `index.ts` get the `validate(schema)` middleware added. Controllers lose their manual validation blocks.

**Tech Stack:** Zod, Express, TypeScript

---

### File Structure

```
server/src/
  middleware/
    validate.ts            ← Express middleware: schema.safeParse(req.body) → 400 on fail
  schemas/
    transaction.schema.ts  ← createTransaction, batchDelete schemas
    account.schema.ts      ← createAccount, updateAccountInfo, updateBalance, createTransfer schemas
    alert.schema.ts        ← createPriceAlert, updatePriceAlert schemas
    budget.schema.ts       ← setBudget schema
    auth.schema.ts         ← lineLogin, guestLogin, refreshToken schemas
    user.schema.ts         ← updateUserSettings, upsertAsset, reduceAsset, importAsset schemas
```

---

### Task 1: Install Zod and create validate middleware

**Files:**
- Modify: `server/package.json` (via npm)
- Create: `server/src/middleware/validate.ts`

- [ ] **Step 1: Install Zod**

```bash
cd server && npm install zod
```

- [ ] **Step 2: Create the middleware**

Create `server/src/middleware/validate.ts`:

```typescript
import { Request, Response, NextFunction } from 'express';
import { ZodSchema, ZodError } from 'zod';

export function validate(schema: ZodSchema) {
  return (req: Request, res: Response, next: NextFunction) => {
    const result = schema.safeParse(req.body);
    if (!result.success) {
      const messages = result.error.errors.map(e => `${e.path.join('.')}: ${e.message}`).join(', ');
      return res.status(400).json({ success: false, error: messages });
    }
    req.body = result.data;
    next();
  };
}
```

- [ ] **Step 3: Verify TypeScript compiles**

```bash
cd server && npx tsc --noEmit
```
Expected: no errors

---

### Task 2: Create transaction schemas

**Files:**
- Create: `server/src/schemas/transaction.schema.ts`
- Modify: `server/src/controllers/transactionController.ts`
- Modify: `server/src/index.ts`

- [ ] **Step 1: Create the schema file**

```typescript
import { z } from 'zod';

export const createTransactionSchema = z.object({
  type: z.enum(['income', 'expense']),
  amount: z.number().positive('Amount must be positive'),
  category: z.string().min(1, 'category is required'),
  note: z.string().optional(),
  accountId: z.string().optional(),
  originalCurrency: z.string().optional(),
  exchangeRate: z.number().optional(),
  date: z.string().optional(),
});

export const batchDeleteTransactionsSchema = z.object({
  transactionIds: z.array(z.string()).min(1, 'transactionIds must be a non-empty array'),
  lineUserId: z.string().min(1, 'lineUserId is required'),
  skipBalanceUpdate: z.boolean().optional(),
});
```

- [ ] **Step 2: Remove manual validation from `transactionController.ts`**

In `createTransaction`, delete lines 41–54 (the `if (!type || !amount || !category)`, `if (!['income','expense'].includes(type))`, and `if (amount <= 0)` blocks). Zod now guarantees these invariants before the handler runs.

- [ ] **Step 3: Add middleware to routes in `index.ts`**

In `server/src/index.ts`, add import:
```typescript
import { validate } from './middleware/validate.js';
import { createTransactionSchema, batchDeleteTransactionsSchema } from './schemas/transaction.schema.js';
```

Update the relevant routes:
```typescript
app.post('/api/transactions/batch-delete', authenticateToken, validate(batchDeleteTransactionsSchema), apiController.batchDeleteTransactions);
app.post('/api/transactions/:lineUserId', authenticateToken, requireLineUserOwnership, validate(createTransactionSchema), apiController.createTransaction);
```

- [ ] **Step 4: Write tests**

Create `server/src/middleware/validate.test.ts`:

```typescript
import { describe, it, expect, vi } from 'vitest';
import { Request, Response, NextFunction } from 'express';
import { z } from 'zod';
import { validate } from './validate.js';

const schema = z.object({
  amount: z.number().positive(),
  type: z.enum(['income', 'expense']),
});

function makeReqRes(body: unknown) {
  const req = { body } as Request;
  const res = {
    status: vi.fn().mockReturnThis(),
    json: vi.fn().mockReturnThis(),
  } as unknown as Response;
  const next = vi.fn() as NextFunction;
  return { req, res, next };
}

describe('validate middleware', () => {
  it('calls next() for valid body', () => {
    const { req, res, next } = makeReqRes({ amount: 100, type: 'expense' });
    validate(schema)(req, res, next);
    expect(next).toHaveBeenCalledOnce();
    expect(res.status).not.toHaveBeenCalled();
  });

  it('returns 400 for missing required field', () => {
    const { req, res, next } = makeReqRes({ amount: 100 });
    validate(schema)(req, res, next);
    expect(res.status).toHaveBeenCalledWith(400);
    expect(next).not.toHaveBeenCalled();
  });

  it('returns 400 for invalid enum value', () => {
    const { req, res, next } = makeReqRes({ amount: 100, type: 'transfer' });
    validate(schema)(req, res, next);
    expect(res.status).toHaveBeenCalledWith(400);
  });

  it('returns 400 for non-positive amount', () => {
    const { req, res, next } = makeReqRes({ amount: -50, type: 'expense' });
    validate(schema)(req, res, next);
    expect(res.status).toHaveBeenCalledWith(400);
  });

  it('coerces req.body to parsed data', () => {
    const { req, res, next } = makeReqRes({ amount: 100, type: 'income', extra: 'ignored' });
    validate(schema)(req, res, next);
    expect(req.body).toEqual({ amount: 100, type: 'income' });
  });
});
```

- [ ] **Step 5: Run tests**

```bash
cd server && npm run test:run
```
Expected: all tests pass (5 new validate tests + existing 114)

- [ ] **Step 6: Commit**

```bash
git add server/src/middleware/ server/src/schemas/transaction.schema.ts server/src/controllers/transactionController.ts server/src/index.ts
git commit -m "feat: add Zod validation middleware for transaction endpoints"
```

---

### Task 3: Create account schemas

**Files:**
- Create: `server/src/schemas/account.schema.ts`
- Modify: `server/src/controllers/accountController.ts`
- Modify: `server/src/index.ts`

- [ ] **Step 1: Create schema file**

```typescript
import { z } from 'zod';

export const createAccountSchema = z.object({
  name: z.string().min(1),
  type: z.enum(['CASH', 'BANK', 'BROKERAGE', 'EXCHANGE']),
  currency: z.enum(['TWD', 'USD']),
  balance: z.number().optional().default(0),
  isDefault: z.boolean().optional().default(false),
  isSub: z.boolean().optional().default(false),
});

export const updateAccountInfoSchema = z.object({
  name: z.string().min(1).optional(),
  isDefault: z.boolean().optional(),
  lineUserId: z.string().min(1),
});

export const updateBalanceSchema = z.object({
  amount: z.number(),
  operation: z.enum(['add', 'subtract']),
  lineUserId: z.string().min(1),
});

export const createTransferSchema = z.object({
  fromAccountId: z.string().min(1),
  toAccountId: z.string().min(1),
  amount: z.number().positive('Amount must be positive'),
  exchangeRate: z.number().optional(),
  fee: z.number().optional(),
  note: z.string().optional(),
});
```

- [ ] **Step 2: Remove manual validation from `accountController.ts`**

- In `createNewAccount`, remove lines 45–58 (the `!name || !type || !currency` and enum checks — Zod now covers these).
- In `updateAccountInfo`, remove lines 160–165 (the `typeof amount !== 'number' || !operation` and operation enum check).
- In `createNewTransfer`, remove lines 207–215 (the `!fromAccountId || !toAccountId || !amount` and `amount <= 0` check).

- [ ] **Step 3: Add middleware to routes in `index.ts`**

```typescript
import { createAccountSchema, updateAccountInfoSchema, updateBalanceSchema, createTransferSchema } from './schemas/account.schema.js';

app.post('/api/accounts/:lineUserId', authenticateToken, requireLineUserOwnership, validate(createAccountSchema), apiController.createNewAccount);
app.patch('/api/accounts/:accountId', authenticateToken, validate(updateAccountInfoSchema), apiController.updateAccountInfo);
app.post('/api/accounts/:accountId/balance', authenticateToken, validate(updateBalanceSchema), apiController.updateBalance);
app.post('/api/transfers/:lineUserId', authenticateToken, requireLineUserOwnership, validate(createTransferSchema), apiController.createNewTransfer);
```

- [ ] **Step 4: Run tests**

```bash
cd server && npm run test:run
```
Expected: all pass

- [ ] **Step 5: Commit**

```bash
git add server/src/schemas/account.schema.ts server/src/controllers/accountController.ts server/src/index.ts
git commit -m "feat: add Zod validation for account endpoints"
```

---

### Task 4: Create remaining schemas (alert, budget, auth, user)

**Files:**
- Create: `server/src/schemas/alert.schema.ts`
- Create: `server/src/schemas/budget.schema.ts`
- Create: `server/src/schemas/auth.schema.ts`
- Create: `server/src/schemas/user.schema.ts`
- Modify: `server/src/controllers/alertController.ts`, `budgetController.ts`, `authController.ts`, `userController.ts`
- Modify: `server/src/index.ts`

- [ ] **Step 1: Create `alert.schema.ts`**

```typescript
import { z } from 'zod';

export const createPriceAlertSchema = z.object({
  symbol: z.string().min(1),
  name: z.string().optional(),
  alertType: z.string().min(1),
  threshold: z.number().optional(),
  targetPrice: z.number().optional(),
  direction: z.string().optional(),
  referencePrice: z.number().optional(),
});

export const updatePriceAlertSchema = z.object({
  isActive: z.boolean(),
  lineUserId: z.string().min(1),
});
```

- [ ] **Step 2: Create `budget.schema.ts`**

```typescript
import { z } from 'zod';

export const setBudgetSchema = z.object({
  category: z.string().min(1),
  amount: z.number().positive('Amount must be positive'),
});
```

- [ ] **Step 3: Create `auth.schema.ts`**

```typescript
import { z } from 'zod';

export const lineLoginSchema = z.object({
  accessToken: z.string().min(1),
  displayName: z.string().optional(),
  pictureUrl: z.string().optional(),
});

export const guestLoginSchema = z.object({
  mockUserId: z.string().regex(/^U[0-9a-f]{32}$/, 'Invalid mockUserId format'),
  displayName: z.string().optional(),
});

export const refreshTokenSchema = z.object({
  refreshToken: z.string().min(1),
});
```

- [ ] **Step 4: Create `user.schema.ts`**

```typescript
import { z } from 'zod';

export const updateUserSettingsSchema = z.object({
  enableTWStock: z.boolean().optional(),
  enableUSStock: z.boolean().optional(),
  enableCrypto: z.boolean().optional(),
});

export const upsertAssetSchema = z.object({
  symbol: z.string().min(1),
  name: z.string().min(1),
  type: z.string().min(1),
  quantity: z.number(),
  avgPrice: z.number(),
  currency: z.string().optional(),
});

export const reduceAssetSchema = z.object({
  symbol: z.string().min(1),
  quantity: z.number().positive('Quantity must be > 0'),
});

export const importAssetSchema = z.object({
  symbol: z.string().min(1),
  name: z.string().min(1),
  type: z.string().min(1),
  quantity: z.number().positive(),
  avgPrice: z.number().positive(),
  currency: z.string().min(1),
});
```

- [ ] **Step 5: Remove manual validation from the 4 controllers**

For each controller, remove the `if (!field)` guard blocks that are now covered by the Zod schemas above. Keep any logic that isn't purely field presence / type checking (e.g. ownership checks stay in controllers).

- [ ] **Step 6: Add middleware to all remaining routes in `index.ts`**

```typescript
import { createPriceAlertSchema, updatePriceAlertSchema } from './schemas/alert.schema.js';
import { setBudgetSchema } from './schemas/budget.schema.js';
import { lineLoginSchema, guestLoginSchema, refreshTokenSchema } from './schemas/auth.schema.js';
import { updateUserSettingsSchema, upsertAssetSchema, reduceAssetSchema, importAssetSchema } from './schemas/user.schema.js';

// Auth
app.post('/api/auth/line-login', validate(lineLoginSchema), authController.lineLogin);
app.post('/api/auth/guest-login', validate(guestLoginSchema), authController.guestLogin);
app.post('/api/auth/refresh', validate(refreshTokenSchema), authController.refreshToken);

// User
app.patch('/api/user/:lineUserId', authenticateToken, requireLineUserOwnership, validate(updateUserSettingsSchema), apiController.updateUserSettings);
app.post('/api/assets/:lineUserId/upsert', authenticateToken, requireLineUserOwnership, validate(upsertAssetSchema), apiController.upsertAssetAPI);
app.post('/api/assets/:lineUserId/reduce', authenticateToken, requireLineUserOwnership, validate(reduceAssetSchema), apiController.reduceAssetAPI);
app.post('/api/assets/:lineUserId/import', authenticateToken, requireLineUserOwnership, validate(importAssetSchema), apiController.importAssetAPI);

// Alerts
app.post('/api/price-alerts/:lineUserId', authenticateToken, requireLineUserOwnership, validate(createPriceAlertSchema), apiController.createPriceAlertAPI);
app.patch('/api/price-alerts/:alertId', authenticateToken, validate(updatePriceAlertSchema), apiController.updatePriceAlertAPI);

// Budget
app.put('/api/budgets/:lineUserId', authenticateToken, requireLineUserOwnership, validate(setBudgetSchema), apiController.setBudget);
```

- [ ] **Step 7: Run tests**

```bash
cd server && npm run test:run
```
Expected: all pass (existing tests + 5 validate tests)

- [ ] **Step 8: Build**

```bash
cd server && npm run build
```
Expected: no errors

- [ ] **Step 9: Commit**

```bash
git add server/src/schemas/ server/src/controllers/ server/src/index.ts
git commit -m "feat: add Zod validation for all remaining API endpoints"
```
